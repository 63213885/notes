git config –global user.name xxx

git config –global user.email xxx@xxx.com

git init

git add .
git add XX

git commit -m “给自己看的备注信息”

git commit --amend -m "新的提交信息"  // 修改最近一次提交的备注信息
git push origin master --force  // 如果已经 push 到远程了，修改 commit 后需要 强制推送：

git remote add origin git@git.acwing.com:xxx/XXX.git

git remote -v

git push -u origin master
git push -u origin main
git push

git clone https://github.com/63213885/XXX.git
git clone https://github.com/63213885/XXX.git .

git pull
git pull origin master
git pull origin main
